import { defineConfig } from '@umijs/max';

export default defineConfig({
  antd: {},
  request: {},
  targets: {
    ie: 11,
  },
  dva: {},
  mfsu: false,
  layout: {
    title: 'pro-blocks',
  },
});
